# =============================================================================
# DOCKER DEPLOYMENT CONFIGURATION
# =============================================================================
# This is a template for Docker deployments. 
# Copy to .env and update the secrets before running docker-compose up
#
# Quick setup:
# 1. cp .env.docker .env
# 2. Generate secrets: openssl rand -base64 32
# 3. Replace JWT_SECRET and MEILISEARCH keys below
# 4. Update ORIGIN to your domain
# 5. Run: docker-compose up -d
#
# =============================================================================

# =============================================================================
# DATABASE (Docker Configuration)
# =============================================================================
DATABASE_URL="file:/app/data/db/sqlite.db"

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# ⚠️ REQUIRED: Generate a secure secret for production!
# Run: openssl rand -base64 32
JWT_SECRET="CHANGE-THIS-generate-with-openssl-rand-base64-32"

# =============================================================================
# MEILISEARCH (Docker Configuration)
# =============================================================================
# Uses Docker service name for container-to-container communication
MEILISEARCH_HOST="http://meilisearch:7700"

# ⚠️ REQUIRED: Generate a secure secret for production!
# Run: openssl rand -base64 32
# Note: Both keys must match
MEILISEARCH_KEY="CHANGE-THIS-generate-with-openssl-rand-base64-32"
MEILISEARCH_MASTER_KEY="CHANGE-THIS-generate-with-openssl-rand-base64-32"

# =============================================================================
# FILE STORAGE (Docker Configuration)
# =============================================================================
BOOKS_STORAGE_PATH="/app/data/books"

# =============================================================================
# APPLICATION (Production Configuration)
# =============================================================================
NODE_ENV="production"
PORT="3000"

# ⚠️ UPDATE THIS: Set to your actual domain in production
# Examples:
#   - Local testing:  http://localhost:3000
#   - Production:     https://books.yourdomain.com
ORIGIN="http://localhost:3000"

# =============================================================================
# DEPLOYMENT CHECKLIST
# =============================================================================
# Before deploying:
# [ ] Generated unique JWT_SECRET
# [ ] Generated unique MEILISEARCH keys
# [ ] Updated ORIGIN to production domain
# [ ] Configured reverse proxy with HTTPS (recommended)
# [ ] Set up regular database backups
# [ ] Reviewed docker-compose.yml port mappings
# =============================================================================

